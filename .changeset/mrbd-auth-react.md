---
"@mrbd/auth": minor
---

First public release. Add the `@mrbd/auth/react` entry point with `MrbdAuthProvider`, `useMrbdAuth`, `MrbdSignInScreen`, `MrbdOtpNumpad`, and `MrbdAuthGate` for adding the MRBD-hosted device-pairing sign-in flow to React glasses apps. `react` is an optional peer dependency.
