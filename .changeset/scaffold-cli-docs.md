---
"create-mrbd-app": patch
---

Document the developer-account CLI in the scaffolded `AGENTS.md`: how to register and edit auth apps with `mrbd login` and `mrbd apps` instead of (or alongside) the web portal.
