---
"@mrbd/react": minor
---

Add a number row (1–0) to the default head keyboard layout, above the letters, so digits can be typed without a separate symbol mode. Also exports `MRBD_DEFAULT_NUMBERS`.
