---
"@mrbd/auth": minor
"create-mrbd-app": patch
---

Simplify the built-in managed auth UI to a black-and-white, sharp-edged style. Add `MrbdOtpInput` for keyboard surfaces with full paste support; `MrbdEmailSignInScreen` now uses it instead of the glasses numpad. Improve OTP paste on `MrbdOtpNumpad` via a focusable overlay input.
